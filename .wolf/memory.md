# Memory

- 2026-07-01: 🪝 **PR-7.2 Hooks 生命周期事件完成** — 5 事件集 (SessionStart/UserPromptSubmit/PreToolUse/PostToolUse/Stop)。`on(event, handler)` 类型化API，每事件独立输入类型。SessionStart injects context via buildAgentOptions，UserPromptSubmit 可 block/replace prompt + inject context，Stop fire-and-forget。3 个退出点触发 Stop (prompt blocked / clarification exit / normal completion)。完全向后兼容：onToolBefore/onToolAfter/runBefore/runAfter 保留。ProviderMessage role 加 "system"。tsc 0 errors, 169+40 tests pass。
- 2026-07-01: 🏗️ **PR-7.1 Gate 边界重划完成** — v0.3.1 本地。三层边界 (hooks/policy/semantic) + 统一 source 前缀。新增 `gate-layer.ts` (GateLayer enum, classifyGateSource, GATE_MANIFEST 25 gates)。所有 source 字符串加层前缀：hooks: (writeGuard, safety-policy, journalGuard), policy: (rate_limit, permission, readonly_intent, ripple_block, planning_phase, context_readiness, web_search_failed, mode_contract, tool_risk), semantic: (ripple_exit, planning_*, task_tracker, quality, external_completion, flash_judge, evidence, truthfulness, master_plan)。GateTelemetry.report() 新增 per-layer 统计。更新 9 个源文件 + 5 个测试文件。tsc 0 errors。
- 2026-06-28: 🔒 **ShellSideEffectGuard 接入 loop.ts** — P0 死接线修复。2 个注入点：(1) pre-execution 在 executeStream 和非 streaming path，danger severity → 阻断执行；(2) post-execution 在 tool result 推入 resultsContent 前，warning severity → 将 `formatSideEffectReport` 追加到 resultContent 供 agent 感知。sideEffectBlocked 变量 per-tool-call 作用域内标记阻断状态。gate_scenario_audit.test.ts 更新（GAP→FIXED），gate-scenario-matrix.md 更新（dead wiring→wired），anatomy.md 更新。176 tests pass, tsc 0 errors。
- 2026-06-28: ✅ **agent_loop 40 tests 全部通过 + ripple replay 79 tests** — 两个优先级修复：(1) agent_loop 集成测试 34→0 fail，添加 `DEEPSEEK_FLASH_TRIAGE=off` 模块级关闭，更新所有过时断言适配 ContextReadiness Gate + ContextMap 注入 + Delivery Report 新格式 + 规划阶段状态消息。测试超时从默认 5s 提升至 10-30s。(2) replay ripple JSON 案例兼容：`obligationsFromReport()` 添加 null guard `report.apiChanges?.length ?? 0`，79 tests 0 fail。tsc 0 errors。
- 2026-06-28: 🧠 **FlashTriage 语义分类默认开启** — `shouldUseFlashTriage` auto-policy 从"含触发词才调API"改为"所有有意义prompt都调API"。仅跳过琐碎续句（好/继续/ok/yes 等）和 <8 chars 无项目上下文的 prompt。关键词分类器降级为真正 fallback（circuit breaker / 网络故障时才启用）。成本：每次 session 首轮多一次 Flash API 调用（~200-500 Flash tokens），后续轮次在 circuit breaker 机制下复用结果。flash-triage.test.ts 更新。
- 2026-06-28: 🐛 **classifyIntent 2个关键词分类 bug 已修复** — Bug 1: "帮我设计X，不需要实现" 中的 "实现" 命中 EXECUTE → `narrow_edit` (误给写权限)。修复：NO_WRITE_PATTERNS 加 `/不(?:需要|必|用|打算)(?:实现|写|修改|执行|改|做|动)/i` 否定模式。Bug 2: "跑一下 bun test 看看有没有失败" 中的 "看看" 命中 DISCUSSION、但 "跑" 不在 EXECUTE → `readonly` (误拒执行)。修复：EXECUTE_PATTERNS 加 `/跑(?:一下|一次|个)?/i` 和 `/运行/i`。57 tests pass, tsc 0 errors。gate-scenario-matrix.md 更新。
- 2026-06-28: 🔍 **Gate 场景审计完成** — `tests/gate_scenario_audit.test.ts` 53 tests, 0 fail, 真实跑通5场景gate评估（不含API调用）。核心发现：(1) classifyIntent 2个bug：架构提示词无NO_WRITE关键词→narrow_edit、shell执行含"看看"→readonly；(2) ShellSideEffectGuard 18 patterns 29 tests 功能正常但0处接入loop.ts（dead wiring）；(3) 只读场景完成链全部pass（0 blocks），写场景ripple/quality正确触发block；(4) 预轮5 gates + 工具策略9 checks + 完成4 gates 每场景均运行，但只读场景成本几乎为零（sync O(1)，无FlashJudge API调用）。文档更新：docs/gate-scenario-matrix.md 新增verified data段。
- 2026-06-28: 🏷️ **版本号 v0.2.2 → v0.3.0** — package.json、src/index.ts、src/runtime/bootstrap.ts、src/ui/cli.ts、src/tui/app.tsx、src/tui/logo.tsx、CHANGELOG.md、docs/v1.0-roadmap.md、docs/design-philosophy.md、docs/awesome-deepseek-agent/*.md 全部同步。Phase 0-6 完成，265+ tests pass, tsc 0 errors。
- 2026-06-28: 🎉 **Phase 6 ModelRouter / Provider 完成** (PR-6.1~6.5). PR-6.1 ModelRouter purpose routing (cheap sub-calls → flash, same-provider preference, 16 tests). PR-6.2 ProviderCapabilities (ModelCapabilities 8-field interface, all 6 built-in models populated, capability query methods, 21 tests). PR-6.3 DeepSeekTranscriptManager (unclosed chain + adjacency + tool limit validation, 31 tests). PR-6.4 JSON/Zod unified structured output (fail-closed callWithStructuredOutput + zodToJsonSchema + objectSchema, 23 tests). PR-6.5 FIM safety constraints (checkFimSafety with forbidden/scope/TOCTOU guard, 26 tests). **265 tests pass, tsc 0 errors, 0 regressions**. Phase 5 post-review fixes: .key$ pattern narrowed to SSH key filenames, tool_risk added to gate telemetry array.
- 2026-06-27: 🎉 **Phase 5 工具风险/权限/沙箱 完成** (PR-5.1~5.5). PR-5.1 ToolRiskTaxonomy (Risk 0-5, 40 tests). PR-5.2 Permission UX (source/priority trace 9 gates, confirm-stubs). PR-5.3 ShellSideEffectGuard (18 patterns, 29 tests). PR-5.4 SecretRedaction 全链路 (unified redactor, run-trace 接入, 38 tests). PR-5.5 SandboxCapability 可见化 (6 features, OS banner, 12 tests). 370 tests pass, tsc 0 errors, 0 regressions.
- 2026-06-27: PR-5.2 Permission UX 完成。ToolPolicyBlocked 新增 source (gate 名) 和 priority (1-8) 字段，所有 9 个 gate block return 均输出 source+priority。创建 confirm-stubs.ts (ConfirmRequest/formatCliConfirmPrompt/TuiConfirmDialogState/ConfirmResult)，实际 TUI 渲染留到 Phase 9。291 tests pass, tsc 0 errors。
- 2026-06-27: PR-5.1 ToolRiskTaxonomy 完成。Risk 0-5 五级分类 (~30 工具映射 + 5 个 Risk-5 param patterns)。PermissionGate.check() 新增 riskLevel 参数阻断 Risk 4-5 session allow。policy.ts Gate 8 (tool_risk) 在所有 gate 之后阻止 Risk 4-5 full-mode 自动批准。40 tests (tool_risk.test.ts), tsc 0 errors, 293 key tests 0 回归。
- 2026-06-27: 🎉 Phase 4 PatchTransaction + Rewind 完成 (PR-4.1~4.4). PR-4.1 状态机 (proposed→committed→rolled_back, 65 tests). PR-4.2 原子替换 (write_file/edit_file/multi_edit 接入 applyAndCommit, 2 CRITICAL fixed). PR-4.3 Unified Rewind (/rewind 命令, 3 modes, 13 tests). PR-4.4 Checkpoint UUID + TUI stubs. 78/78 tests pass, tsc 0 errors. edit_fim 留到后续 PR 迁移。
- 2026-06-27: PR-4.2 PatchTransaction Phase 2 原子替换完成。write_file/edit_file/multi_edit 全部接入 applyAndCommit，原子 temp→verify→commit。禁止写入强制生效。2 CRITICAL (txId 格式不匹配→改用 fileTransaction.id; applyAndCommit 吞错误→改为 re-throw) + 2 HIGH review 发现全部修复。65/65 tests pass, tsc 0 errors。edit_fim 未迁移。
- 2026-06-27: PR-4.1 PatchTransaction 状态机完成。proposed→applied_to_temp→verified→committed→rolled_back 完整状态机，VALID_TRANSITIONS 强制合法转换。initManagedTransaction 内置 forbidden-file gate。commitManagedTransaction 有 base-hash TOCTOU guard。applyAndCommit 全生命周期 + verify 回调。模块级 txRegistry 自动在 commit/rollback 后清理。4 HIGH + 2 MEDIUM review 发现全部修复。64/64 tests pass, tsc 0 errors。NOT yet wired into tools/file.ts — 留给 PR-4.2。
- 2026-07-01: Phase 3 Completion Orchestrator + Evidence 完成。PR-3.1 CompletionOrchestrator 统一所有 final gate 为单一 evaluate() 方法，loop.ts ~180 行→~50 行。PR-3.2 canClaimDone() 成为唯一证据判定入口，evaluateCompletionGate 重复检查移除。PR-3.3 FinalTruthfulnessGate 加入未来时态过滤减少误报。4 HIGH + 2 MEDIUM review 发现全部修复。窄编辑自动完成加入 canClaimDone 硬检查。修复 mode-contract shouldTransitionMode 预存 bug。
- 2026-06-27: Phase 2 MasterPlan/ModeContract/TaskPacket 完成。PR-2.1 TaskPacket 验证 (validateTaskPacket 9 字段检查，createTaskTrackerFromPacket fail-closed)。PR-2.2 ModeContract 自动流转 (shouldTransitionMode 4 规则：planComplete→report/toolErrors≥3→repair/ripple>0→stay/node status→mode mapping)。PR-2.3 TaskPacket 接管 (buildTaskTrackerFromPrompt，长任务走 packet 路径)。PR-2.4 PlanValidator 强化 (+checkNonEmpty/+checkDependencyValidity/+checkTitleQuality)。新增 10 shouldTransitionMode tests。修复 3 预存 test regressions。tsc 0 errors。
- 2026-06-27: PR-0.2 文档真实性修复。修正 master-plan.ts 顶部过时注释（"NOT YET WIRED" → 准确描述接线状态和剩余 gap）。ARCHITECTURE.md 新增模块状态表（25 行，🟢 stable/🟡 partial/🔵 planned 分类）+ 4 个章节标注。SECURITY.md 扩充（macOS/Linux sandbox 降级说明、MCP RCE 风险与缓解、工具风险分级、Secret Redaction 状态、Ripple Obligation Gate、Runtime Self-Edit Gate），版本表更新。v0.2.2 已支持。
- 2026-06-27: PR-0.1 Runtime Bootstrap 完成。创建 src/runtime/bootstrap.ts — 统一 CLI/TUI 装配。CLI 移除 ~80 行手动装配，改为 createRuntime() 单调用。TUI 从只有 DeepSeekProvider + 子集 tools 升级到完整 MultiProvider + MCP/LSP/TypeScript + HookSystem + ThinkingStore + KnowledgeBase + StagedContextManager。tsc 0 errors。
- 2026-06-27: v0.2.2 版本基线确立 + v1.0 完整路线图。版本漂移修复（CLI 0.3.0/TUI 0.4.0/index.ts 0.2.1 → 统一 0.2.2，6 处修改）。基于 Deep Research 绝对性审查报告创建 docs/v1.0-roadmap.md（10 Phase/32+ PR/8-10 周），含模块逐项审查表（25 模块）、P0/P1/P2 优先级矩阵、PR 模板、TUI 专项、Orcana 风格保留。更新 .wolf/anatomy.md。
- 2026-06-24: Created GateTelemetry class (src/agent/gates/telemetry.ts) — pure data collector with record, markFalsePositive, markMissed, report, toJSON/fromJSON, merge. Wired into GateChain via optional `telemetry?` parameter. 33 unit tests, tsc 0 errors, 0 regressions.
- 2026-06-24: Code review fixes — fromJSON validates all fields are finite numbers (skips malformed), markFalsePositive guards against blocks===0 (definitionally impossible), inlined redundant Map lookups in report/summary.
- 2026-06-24: Test coverage — added evaluateWithTrace block-early test, merge disjoint sets test, fromJSON validation test, async evaluate undefined telemetry test, ContextBudgetGate block test, RippleExitGate block test.
- 2026-06-24: Wired GateTelemetry into loop.ts — AgentOptions.gateTelemetry, preRoundChain.evaluateSync(ctx, tel), completionChain.evaluateSync(ctx, tel), tool policy manual recording, yield telemetry status at loop end. tsc 0 errors.
- 2026-06-24: Step 1 telemetry report — 15 scenarios, 14 gates instrumented. 3 pass-through by design (tool_disclosure, readonly_plan, ripple_tool_filter). 11 blocking gates each have a known failure case. Data is synthetic — need 50+ real tasks for true intercept rates.
- 2026-06-24: Step 2 telemetry batch — 20 scenarios (14 block + 6 all-pass), 14 gates instrumented. Gates classified: 3 pass-through filters (never block), 4 guards (block violations), 3 verify (block unverified completion), 4 safety (system protection). Intercept rates synthetic (6 all-pass inflates denominators). Live API data needed for real rates.
- 2026-06-24: GateTelemetry fully integrated — loop.ts yield telemetry report on agent exit. Ready for production collection.
- 2026-06-24: Step 3 tooling — manifest.ts (generateManifest + manifestReport, decision matrix encoded as code), GateTelemetry.saveToFile/loadFromFile (cross-session persistence), AgentOptions.gateTelemetryFile (auto-save on exit). 13 new tests, 46/46 pass, tsc 0 errors. Gate Manifest v1 generated from synthetic data: 3 safety_net + 3 pass_through + 8 observe (all 5-14% due to n=1 synthetic blocks). Real data will produce keep/tune/delete decisions.
- 2026-06-24: Code review fixes (3 HIGH + 2 MEDIUM) — decision matrix gap closed (fpRate >= 0.10 → tune), flushTelemetry helper at all 3 exit points with mkdir + proper error handling, loadFromFile type guard on JSON.parse result, JSDoc typo fixed.
- 2026-06-24: Wired gateTelemetryFile into CLI and TUI entry points (.wolf/gate-telemetry.json). Updated loop-types.ts AgentOptions with gateTelemetry/gateTelemetryFile/modelRouter fields. tsc 0 errors, 46/46 tests pass. Every agent run now auto-saves telemetry.
- 2026-06-24: Telemetry accumulation — at startup, auto-load previous .wolf/gate-telemetry.json and merge into new instance (additive). Data now accumulates across runs instead of being overwritten each time.
- 2026-06-24: Audit debt fixes — removed package-lock.json (Bun-only), moved parse_trending.mjs to scripts/, added DEEPSEEK_MAX_ROUNDS env var, softened "zero identity bias" claim to "reduced identity bias", documented FIM /beta endpoint risk, documented Gate Overflow prompt-only limitation, added context to Do-Not-Repeat #1.
- 2026-06-24: Audit round 2 fixes — removed dead _usage type-penetration (written never read), replaced [PLAN_APPROVED] string protocol with AgentOptions.initialPlanState, Flash Triage default changed to "auto" (now matches design intent), removed duplicate fields in taskTracker spread, added flashTriagePolicy override to AgentOptions, created CHANGELOG.md. tsc 0 errors, all tests pass, 0 regressions.
- 2026-06-24: GitHub 项目页信号建设 — 创建 5 个有源码行号引用的真实 Issue，README 添加 npm 版本/下载量/stars/issues/Bun 等 7 个 badge，解决 0 star/0 fork/0 issue 的空仓外观。
- 2026-06-24: 搜索后端升级 — web_search 从 SearXNG Docker + DuckDuckGo 切换为 Exa 语义搜索（免费 1000次/月，REST API + MCP fallback），web_fetch 从 regex stripHtml 切换为 Jina Reader（免费 200 req/min，Markdown 提取）。零 Docker 依赖，纯 HTTP。tsc 0 errors，全量测试 pass。
- 2026-06-24: 补测 3 个零覆盖门禁 — Rate Limiter (21 tests, tool_policy.test.ts), Gate Overflow (17 tests, gate_overflow.test.ts), Revise Plan (15 tests, revise_plan.test.ts)。tsc 0 errors，全量 0 regressions。
- 2026-06-24: AgentOptions 双份定义合并 — 删除 loop.ts 本地定义，统一 import loop-types.ts。UsageStats 同步合并。修复所有预存测试类型错误（ToolPolicyResult discriminated union, RippleObligation 类型）。tsc 0 errors。
- 2026-06-24: slice(-24) 硬编码替换为 context-budget-aware 计算 — 150K token 预算（~15% of 1M），按字符数估算 token，最多保留 60 条历史。tsc 0 errors，全量 pass。
- 2026-06-24: Orcana 多 agent 架构决策 — T3R (Planner/Coder/Reviewer) + Locator/Verifier 微 agent。当前单 agent → 三角色 mode → 拆真 agent。先做协议（TaskPacket/PatchTransaction/ReviewResult/AgentEvent），再拆 agent。Runtime 是非模型的权力中心。详见 .wolf/orcana-architecture.md。已同步写入 Obsidian。
- 2026-06-24: 用户纠正 — 重大产出必须主动写 Obsidian，不能等人问。已写入 cerebrum.md Do-Not-Repeat。
- 2026-06-24: 下一阶段决策 — 不做多 agent，先硬化单 agent 长程运行时。9 个 PR（MasterPlan 接入→TaskPacket→Context Epoch→PatchTransaction→Evidence→Ripple→ModeContract→Replay Harness）。协议硬了再拆 agent。详见 .wolf/orcana-next-phase.md。
- 2026-06-24: PR 1 完成 — MasterPlan 接入 loop.ts。long_task 计划接受后自动创建 MasterPlan 并替换 taskTracker 为当前节点 tracker。节点完成后通过 buildNodeReviewGate 过渡到下一节点。所有节点完成前阻止最终 done。3 HIGH review 问题已修复（节点 tracker phase 修正、blocked-node 继续处理、planText 跨会话持久化）。20 新测试，0 回归。
- 2026-06-25: PR 2 完成 — TaskPacket 驱动 TaskTracker。新增 task-packet.ts（TaskPacket/VerificationRequirement/RipplePolicy/ContextBudget 类型，extractScopeFromLine 启发式解析，buildPacketFromLine + createTaskTrackerFromPacket）。createMasterPlan/addNode 改为 packet 驱动。updateTaskTrackerAfterTools 增加 skipLegacyStepIds 参数。createTaskTracker 标记 @deprecated。57 测试（29 task_packet + 20 master_plan + 8 task_tracker），0 回归。2 HIGH + 3 MEDIUM review 问题已修复（FILE_PATH_RE 虚警防护 + dotfile 支持、step-ID 不匹配、command 字段填充、doneCriteria 语义）。
- 2026-06-25: PR 4 完成 — Context Epoch 四层上下文架构。新增 context-epoch.ts：EpochState/EpochThresholds/epochRollover/hasUnclosedToolChain/buildPlanStateContext/classifyEpochAction。120k 压缩→220k 强制压缩→300k epoch rollover。Plan State 层（MasterPlan/TaskTracker/Ripple 快照）在 rollover 后保留。DeepSeek 未闭合 tool-use 链防护（防 HTTP 400）。loop.ts 阈值检查、rollover 逻辑、force-compress 警告全接入。request-builder.ts 去重 msgCharLen。34 新测试，0 回归，总计 160 tests pass。
- 2026-06-25: PR 5 完成 — PatchTransaction 事务化写入层。新增 patch-transaction.ts：txId/baseHash/diff/scope/verification。base hash 校验防 mid-air conflict，forbiddenFiles 防写入 .git/.deepseek-code/node_modules 等，case-insensitive 防 Windows 绕过。setActivePatchContext 在 3 个节点激活点接线。write_file/edit_file/multi_edit/edit_fim 全部接入 preWriteCheck。revisePlan 修复 _packet 缺失（防 plan revision 后 stale context）。checkpointMetadata 去重 hash 逻辑。35 新测试（含 Windows 绕过用例），0 回归，总计 248 tests pass。
- 2026-06-25: PR 6 完成 — Evidence Ledger 结构化验证证据。新增 evidence-ledger.ts：EvidenceKind（typecheck/test/build/manual 四类）、EvidenceEntry/EvidenceLedger、toEvidenceKind 映射（lint→typecheck、smoke→test）、canClaimDone 硬检查（步骤+文件+证据）、ingestVerificationResults 批量摄入、addManualEvidence 人工证据、format/serialize 工具。接入 loop.ts（创建+传入 updateTaskTrackerAfterTools+传入 evaluateCompletionGate）、task-tracker.ts（可选 evidenceLedger 参数）、completion-gate.ts（CompletionGateInput.evidenceLedger）。证据检查集成到 evaluateCompletionGate 而非独立 gate（避免冗余和 gate 顺序歧义）。Known Phase 1 限制：无证据新鲜度追踪（累积式账本）、与 verificationEvidence 双写（向后兼容重）、manual 证据仅咨询。44 新测试，0 回归，总计 292 tests pass。
- 2026-06-25: PR 7 完成 — Ripple Obligations 接 Completion Gate。Ripple 从提示升级为硬义务：新增 RippleWaiver（reason+timestamp）、waiveObligation（空原因拒绝）、isObligationBlocking、getBlockingObligations、formatWaivedObligations。RippleExitGate 改用 getBlockingObligations（仅非豁免义务阻塞）。evaluateCompletionGate 改用 getBlockingObligations。修复 3 处遗漏路径：autoFinishOnVerifiedWrite（loop.ts:1334）、processGateOverflow（loop.ts:1360）、strongestRippleDecision（pre-round.ts:81）——之前都用了 raw .length 而非 blocking count。mergeObligations 设计：新 report 覆盖旧 waiver（stale waiver, fresh finding）；resolveObligations 保留 waiver。Known Phase 1 限制：waiveObligation 无生产调用方（豁免仅通过 resolveObligations 自动创建），工具/提示词通路延后。32 新测试，tsc 0 errors，275 tests pass（0 回归）。
- 2026-06-25: PR 8 完成 — ModeContract 单 agent 五模式角色纪律。新增 mode-contract.ts：ModeName/ModeContract/ModeExitCriterion 类型，5 个模式定义（planner/coder/review/repair/report），enforceModeTools（forbiddenTools 优先，MCP 工具自动放行），checkModeExitCriteria（no_tool_errors/output_not_empty/has_evidence），formatModePrompt（每轮注入），setActiveMode/getActiveMode 模块级状态。集成点：policy.ts Gate 7（mode_contract），completion-gate.ts 退出条件检查+needsExternalCompletionGate 修复（防绕过），loop.ts 状态初始化+上下文注入+遥测。Code review 修复：HIGH-1 request_deeper_thinking+MCP 工具分类（META_TOOLS + mcp__* 前缀通配），HIGH-2 退出条件绕过（needsExternalCompletionGate 始终检查 mode exit criteria），HIGH-3 EvidenceKind 类型安全。Phase 1 限制：mode transitions 未接入 MasterPlan 节点切换，shouldTransitionMode 为 stub。59 新测试，tsc 0 errors，383 tests pass（0 回归）。
- 2026-06-25: PR 9 完成 — Replay Harness 30 cases。新增 replay-harness.ts：ReplayDomain/ReplayExpected（5 域 discriminated union）/ReplayCase/ReplaySuite 类型，checkAssertions 断言引擎（exists/equals/gt/gte/contains）。30 个 JSON case 文件分布在 tests/replay/ 的 5 个域目录中（每域 6 cases），覆盖：master-plan（create/transition/complete/blocked/force-pass/validation），context-epoch（阈值的 4 个 actions + tool-chain guard + plan state preserved），false-done（missing-verification/ripple-blocking/tracker-incomplete/mode-exit-fail/typecheck-failed/clean-completion），ripple（obligations/waive/blocking-filter/merge/resolve/cascade），patch-transaction（allowed/forbidden-git/forbidden-node-modules/hash-mismatch/diff/serialization）。tests/replay_harness.test.ts 加载全部 30 cases，dispatchCase 将 targetFunction 字符串映射到实际模块函数调用，validateResult 做 domain-specific 验证。Fixture 系统创建临时目录和文件支持需要文件系统的 tests（checkBaseHash、createPatchTransaction）。关键设计决策：(1) JSON 数据驱动 + TypeScript 类型安全 —— case 文件是纯 JSON（满足 PR 要求），但 ReplayExpected 类型在编译时检查结构一致性；(2) dispatchCase 用 switch 手动映射而非动态 import —— 因为 JSON 无法携带函数引用，switch 是显式、可测的映射；(3) 断言引擎用简单字符串解析（exists/equals/gt/gte/contains）—— 覆盖 80% 验证需求，复杂断言走 domain-specific 验证；(4) 模块级状态在每 case 前重置 —— planRef.current=null + setActiveMode("coder")，确保 cases 间隔离。37 tests pass（30 cases + 7 structural），tsc 0 errors，321 tests pass across key modules（0 回归）。🎉 9-PR 单 agent 长程运行时骨架全部完成。
- 2026-06-26: ContextMap runtime 集成完成 — agentLoop 新增 `contextMapPolicy`（off|auto|always，默认 auto），长任务/高风险/显式文件任务会预先 buildContextMap，把摘要注入 stable prefix，并把 `contextMapId`/`requiredContextEvidence` 附到 MasterPlan/TaskPacket。MasterPlan 改为所有节点都走 `buildPacketFromLine` + `createTaskTrackerFromPacket`，`addNode`/`revisePlan` 继承 context evidence，`serializePlan` 输出 packet scope/verification/context/validation。新增 ContextReadiness pre-round gate + tool policy gate：高风险 readiness 不足时 provider 侧隐藏写工具，执行侧也阻断非 readonly 工具；cache-stable tools 不再绕过该安全过滤。新增 tests/context_map_runtime.test.ts 与 tests/pre_round_context_readiness.test.ts。验证：focused 115 tests pass，tsc pass，bun run test pass，bun run build pass。
- 2026-06-26: Ripple PR 2 完成 — ApiChange 替换 changedSymbols。新增 api-diff.ts：SymbolShape 类型（含 nameStart/nameEnd/declStart/declEnd 精度字段）、ApiChangeKind（8 种变更类型）、ApiChange（结构化变更含预计算 severity）、diffApiSurface（结构化 diff 引擎替代旧 changedSymbols()）、toSymbolShapes/changedSymbolNames/hasSeverity 工具函数。engine.ts 变更：SymbolInfo 加精度字段、parseCache 改为缓存 SourceFile+lines（始终走 AST）、extractSymbols 填充 nameStart/nameEnd/declStart/declEnd、verifyCallersSemantically 用 nameStart 替代 findLineStart（已删除）、previewEdit 用 diffApiSurface 驱动 finding 生成（switch on change.kind）、formatRippleBlock 用 apiChanges 生成 kind 标注摘要、tightenRippleDecision 用 hasSeverity、resetRippleProgram 清 parseCache+fileListCache、导出 invalidateFileListCache。types.ts：RippleReport 新增 apiChanges 字段，changedSymbols 标记 @deprecated。修复 test fixtures（agent_loop + ripple + obligations 共 6 处补 apiChanges: []）。27 api-diff 新测试。tsc 0 errors，77 ripple tests + 166 regression tests pass（0 failures）。Ripple Engine 评分：5.5→6.8/10。
- 2026-06-26: Ripple PR 3 完成 — SemanticReferenceProvider 主路径。新增 semantic-reference-provider.ts：SemanticReferenceProvider 类包装 ProjectProgram 作为 PRIMARY caller 发现路径，findCallers 用 TypeChecker 语义引用解析替代文本 grep 作为主入口，resolveSymbol 做别名链解析，全局单例 get/reset。engine.ts 变更：previewEdit 调用语义路径优先 — ready 时用语义结果 + 同文件文本补充，未 ready 退回文本扫描 + verifyCallersSemantically 路径，resetRippleProgram 同步 reset 语义 provider。16 新测试（lifecycle/state/fallback/dedup/singleton/contract）。tsc 0 errors，78 ripple tests + 141 broader tests pass（0 regressions）。Ripple Engine 评分：6.8→7.5/10。
- 2026-06-26: Ripple PR 4 完成 — UsageImpact 分类器。新增 usage-classifier.ts：14 UsageKind（call_expr/method_call/new_instance/type_ref/extends_clause/implements_clause/generic_arg/typeof_query/destructure/jsx_element/jsx_attr/re_export/spread_expr/plain_ref）、UsageImpact 类型（caller/usage/requiredAction/confidence）、classifyOneCaller（按特异性排序的 regex 启发式分类器）、classifyCallers（批量分类 + resolveAction）、resolveAction（UsageKind × ApiChangeKind → 具体 requiredAction，如 async_boundary + call_expr → "add await to this call"）、findPrimaryChange（多变更优先级排序）、formatUsageSummary（按 action 分组 + 文件列表）、urgencyLevel（urgent > actionable > info）。engine.ts 变更：caller 发现后立即运行 classifyCallers、RippleReport 新增 usageImpacts、finding 生成富化（async_boundary 统计 await 数、signature 统计 argument update 数、export_removed 附加 per-caller action）、formatRippleBlock 标注 caller 用法 + 附加 usage summary。types.ts：RippleReport.usageImpacts、re-export UsageKind/UsageImpact。测试 fixtures：4 文件 5 处补 usageImpacts: []。49 新测试，tsc 0 errors，143 ripple tests + 254 broader tests pass（0 regressions）。Ripple Engine 评分：7.5→7.9/10。
37	- 2026-06-26: Ripple PR 6 完成 — Verification Map 验证映射层。新增 verification-map.ts：VerificationStep（type/command/label/coverage/priority）、VerificationMap（targetFile/steps/affectedTestFiles/uncoveredSymbols/coverage）、buildVerificationMap（约定式测试文件发现：tests/<name>.test.ts、tests/<subdir>/<name>.test.ts、__tests__/<name>.test.ts、index→parent 目录）、buildSteps（始终 typecheck、≤3 个独立 test 命令、>3 聚合推荐、async/signature 自定义验证步骤）、formatVerificationMap（按优先级分组 Required→Recommended→Optional、覆盖率警告含未覆盖符号截断至 5）、primaryVerificationCommand（首个 required step 命令）、mergeVerificationMaps（联合+去重+平均覆盖率）、isShallowChange/verificationStrictness（变更深度判断）。engine.ts 变更：previewEdit 在 classifyCallers 后立即 buildVerificationMap、RippleReport 新增 verificationMap、formatRippleBlock 在 usage actions 和 findings 之间插入 verification commands。types.ts：RippleReport.verificationMap? 可选字段、re-export VerificationMap/VerificationStep。47 新测试，tsc 0 errors，190 ripple tests + 307 broader tests pass（0 regressions）。Ripple Engine 评分：7.9→8.4/10。
38	- 2026-06-26: Ripple PR 7 完成 — AstGrep Fallback Provider。新增 astgrep-provider.ts：AstGrepMatch/AstGrepStats 类型、AstGrepProvider（_execFn 依赖注入 + _exec 统一 shell 执行、isAvailable 缓存可用性检查、discoverCallers 6 种模式匹配：import/re_export/new_instance/method_call/call_expr/identifier、runQuery 解析 sg scan --json 输出、exit code 1 空结果/非 JSON 容错/status>1 跳过单模式）、generatePatterns（symbol 内 regex 特殊字符转义）、全局单例 get/reset。engine.ts 变更：previewEdit 在 semantic 或 text 路径后运行 ast-grep 富化（仅 sg 可用时）、结果按 file:line 去重合并、resetRippleProgram 同步 reset。24 新测试（_execFn DI 模式消除跨文件 mock 泄漏），tsc 0 errors，190 ripple tests + 363 broader tests pass（0 regressions）。Ripple Engine 评分：8.4→8.6/10。🎉 Ripple Engine 2.0 四层架构全部完成。
39	- 2026-06-26: Ripple Engine 2.0 全面审计 — 发现 2 HIGH + 3 MEDIUM + 4 LOW 问题并全部修复。H1: obligationsFromReport 改用 apiChanges 替代 deprecated changedSymbols。H2: 删除 engine.ts 中未使用的 isShallowChange 导入。M1: 文档化 SemanticReference 首次调用限制（ensureProgram 异步构建，首次调用始终返回空）。M2: wiring verificationStrictness 到 previewEdit — strict 变更 + 未覆盖符号 → info advisory finding。cascadeAwareDecision 过滤 info 级别 findings 防止误触发。L1: 删除 classifyOneCaller 未使用的 _change 参数和死函数 findPrimaryChange。L2: ast-grep identifier pattern 添加 catch-all 注释。L3: buildSteps hardcoded bun 添加 NOTE 注释。L4: obligations.ts 注释修正 Ripple PR 编号。212 tests pass，tsc 0 errors。评分调整：8.6→8.5/10（接线完整度增加，但首次调用限制拉低了理想分）。
40	- 2026-06-26: Ripple Engine 2.0 文档完成 — 创建 docs/ripple-engine.md（完整英文架构文档，含 7 层 ASCII 架构图、数据流时间线、层详解、集成点、设计决策、已知限制）。更新 README.md / README.zh.md（扩展架构图、新增 ripple 5 层 ASCII 图）。更新 ARCHITECTURE.md（ripple 段重写为 7 层总结）。
- 2026-06-26: Orcana Strong Single v1.0 实行计划入库 — 10 Phase / 32 PR 组，8-10 周路线图。五批次优先级：HookSystem P0 修复→完成闭环→安全工具→模型 provider→可验证产品化。17 条 v1.0 验收标准。计划已写入 Obsidian `projects/Orcana Strong Single v1.0 实行计划.md`。关联：不重复展开 Ripple/Evolution/ContextMemory/ContextMap 四块，预留接口。
